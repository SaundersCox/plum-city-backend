spring:
  datasource:
    url: jdbc:sqlserver://localhost\\MSSQLSERVER01;databaseName=plumcity;integratedSecurity=true
    username: admin
    password: password
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
